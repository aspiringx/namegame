## Basic UI

* In src/MainPage.tsx, create a basic UI for each schema.prisma model
  * Idiomatic form to create the model
  * List existing models
    * Each row should have a button to delete the model
  * Idiomatic form to update the model
